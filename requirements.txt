FreeSimpleGUI==5.3.0.post1
scapy==2.5.0
pyshark==0.6

# for the YARA scanning of extracted HTTP objects. watch out - there's an
# old abandoned package on PyPI just called "yara" (not yara-python) that
# installs the same top-level yara module and'll silently shadow this one
# if it's ever installed alongside it. if `import yara; yara.__version__`
# doesn't start with 4., that's what happened - pip uninstall yara (no
# "-python") and reinstall this
yara-python==4.5.1

# only need this stuff for running tests, not for actually running nids.py
pytest==9.1.1
